dataset:
    path: HuggingFaceFW/fineweb
    split: train
    name: sample-10BT
    streaming: true
collator:
    _target_: collators.tokenize.TokenizeCollator
    tokenizer_url: meta-llama/Llama-3.2-1B
    sequence_length: 1024
    pad_token_id: ${model.pad_token_id}
